{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "env": {
      "NODE_VERSION": "20.11.1"
    },
    "buildCommand": "cd frontend && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "node create-env.js && cd installation && npm ci && node setup.js && cd .. && node server.js"
  }
}
