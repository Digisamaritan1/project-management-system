{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "env": {
      "NODE_VERSION": "20.11.1"
    },
    "buildCommand": "npm run basic-install && cd frontend && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "node create-env.js && cd installation && npm ci && npm run build && cd .. && node server.js"
  }
}
