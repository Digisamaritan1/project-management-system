{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cd frontend && npm ci && npm run build && cd ../installation && npm ci && node setup.js && cd .."
  },
  "deploy": {
    "startCommand": "node server.js"
  }
}
