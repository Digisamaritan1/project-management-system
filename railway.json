{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
  "env": {
      "NODE_VERSION": "20.11.1"
    },
    "buildCommand": "npm install && cd frontend && npm install && cd ../installation && npm install && npm run build && cd .."
  },
  "deploy": {
    "startCommand": "node create-env.js && npm start"
  }
}
